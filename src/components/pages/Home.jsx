import UserResults from "../users/UserResults";
import UserSearch from "../users/UserSearch";
const Home = (props) => {
    return (
        <>
            <UserSearch />
            <UserResults />
        </>
    );
}

export default Home;
